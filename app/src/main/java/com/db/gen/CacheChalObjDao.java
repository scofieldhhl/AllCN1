package com.db.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.datas.CacheChalObj;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "CACHE_CHAL_OBJ".
*/
public class CacheChalObjDao extends AbstractDao<CacheChalObj, Long> {

    public static final String TABLENAME = "CACHE_CHAL_OBJ";

    /**
     * Properties of entity CacheChalObj.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property KeyL = new Property(0, Long.class, "keyL", true, "_id");
        public final static Property Name = new Property(1, String.class, "name", false, "NAME");
        public final static Property ColName = new Property(2, String.class, "colName", false, "COL_NAME");
        public final static Property ColId = new Property(3, String.class, "colId", false, "COL_ID");
    }


    public CacheChalObjDao(DaoConfig config) {
        super(config);
    }
    
    public CacheChalObjDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"CACHE_CHAL_OBJ\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: keyL
                "\"NAME\" TEXT NOT NULL ," + // 1: name
                "\"COL_NAME\" TEXT NOT NULL ," + // 2: colName
                "\"COL_ID\" TEXT NOT NULL );"); // 3: colId
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_CACHE_CHAL_OBJ_NAME_COL_NAME_COL_ID ON \"CACHE_CHAL_OBJ\"" +
                " (\"NAME\" ASC,\"COL_NAME\" ASC,\"COL_ID\" ASC);");
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"CACHE_CHAL_OBJ\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, CacheChalObj entity) {
        stmt.clearBindings();
 
        Long keyL = entity.getKeyL();
        if (keyL != null) {
            stmt.bindLong(1, keyL);
        }
        stmt.bindString(2, entity.getName());
        stmt.bindString(3, entity.getColName());
        stmt.bindString(4, entity.getColId());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, CacheChalObj entity) {
        stmt.clearBindings();
 
        Long keyL = entity.getKeyL();
        if (keyL != null) {
            stmt.bindLong(1, keyL);
        }
        stmt.bindString(2, entity.getName());
        stmt.bindString(3, entity.getColName());
        stmt.bindString(4, entity.getColId());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public CacheChalObj readEntity(Cursor cursor, int offset) {
        CacheChalObj entity = new CacheChalObj( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // keyL
            cursor.getString(offset + 1), // name
            cursor.getString(offset + 2), // colName
            cursor.getString(offset + 3) // colId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, CacheChalObj entity, int offset) {
        entity.setKeyL(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setName(cursor.getString(offset + 1));
        entity.setColName(cursor.getString(offset + 2));
        entity.setColId(cursor.getString(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(CacheChalObj entity, long rowId) {
        entity.setKeyL(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(CacheChalObj entity) {
        if(entity != null) {
            return entity.getKeyL();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(CacheChalObj entity) {
        return entity.getKeyL() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
