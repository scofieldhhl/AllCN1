package com.db.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.datas.LiveCKindObj;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "LIVE_CKIND_OBJ".
*/
public class LiveCKindObjDao extends AbstractDao<LiveCKindObj, Long> {

    public static final String TABLENAME = "LIVE_CKIND_OBJ";

    /**
     * Properties of entity LiveCKindObj.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property KeyL = new Property(0, Long.class, "keyL", true, "_id");
        public final static Property ColName = new Property(1, String.class, "colName", false, "COL_NAME");
        public final static Property ColId = new Property(2, String.class, "colId", false, "COL_ID");
        public final static Property PColName = new Property(3, String.class, "pColName", false, "P_COL_NAME");
        public final static Property PColId = new Property(4, String.class, "pColId", false, "P_COL_ID");
        public final static Property ChalNum = new Property(5, int.class, "chalNum", false, "CHAL_NUM");
        public final static Property PageNum = new Property(6, int.class, "pageNum", false, "PAGE_NUM");
        public final static Property PType = new Property(7, int.class, "pType", false, "P_TYPE");
    }


    public LiveCKindObjDao(DaoConfig config) {
        super(config);
    }
    
    public LiveCKindObjDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"LIVE_CKIND_OBJ\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: keyL
                "\"COL_NAME\" TEXT NOT NULL ," + // 1: colName
                "\"COL_ID\" TEXT NOT NULL ," + // 2: colId
                "\"P_COL_NAME\" TEXT NOT NULL ," + // 3: pColName
                "\"P_COL_ID\" TEXT NOT NULL ," + // 4: pColId
                "\"CHAL_NUM\" INTEGER NOT NULL ," + // 5: chalNum
                "\"PAGE_NUM\" INTEGER NOT NULL ," + // 6: pageNum
                "\"P_TYPE\" INTEGER NOT NULL );"); // 7: pType
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_LIVE_CKIND_OBJ_COL_ID_COL_NAME_P_COL_ID_P_COL_NAME ON \"LIVE_CKIND_OBJ\"" +
                " (\"COL_ID\" ASC,\"COL_NAME\" ASC,\"P_COL_ID\" ASC,\"P_COL_NAME\" ASC);");
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"LIVE_CKIND_OBJ\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, LiveCKindObj entity) {
        stmt.clearBindings();
 
        Long keyL = entity.getKeyL();
        if (keyL != null) {
            stmt.bindLong(1, keyL);
        }
        stmt.bindString(2, entity.getColName());
        stmt.bindString(3, entity.getColId());
        stmt.bindString(4, entity.getPColName());
        stmt.bindString(5, entity.getPColId());
        stmt.bindLong(6, entity.getChalNum());
        stmt.bindLong(7, entity.getPageNum());
        stmt.bindLong(8, entity.getPType());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, LiveCKindObj entity) {
        stmt.clearBindings();
 
        Long keyL = entity.getKeyL();
        if (keyL != null) {
            stmt.bindLong(1, keyL);
        }
        stmt.bindString(2, entity.getColName());
        stmt.bindString(3, entity.getColId());
        stmt.bindString(4, entity.getPColName());
        stmt.bindString(5, entity.getPColId());
        stmt.bindLong(6, entity.getChalNum());
        stmt.bindLong(7, entity.getPageNum());
        stmt.bindLong(8, entity.getPType());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public LiveCKindObj readEntity(Cursor cursor, int offset) {
        LiveCKindObj entity = new LiveCKindObj( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // keyL
            cursor.getString(offset + 1), // colName
            cursor.getString(offset + 2), // colId
            cursor.getString(offset + 3), // pColName
            cursor.getString(offset + 4), // pColId
            cursor.getInt(offset + 5), // chalNum
            cursor.getInt(offset + 6), // pageNum
            cursor.getInt(offset + 7) // pType
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, LiveCKindObj entity, int offset) {
        entity.setKeyL(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setColName(cursor.getString(offset + 1));
        entity.setColId(cursor.getString(offset + 2));
        entity.setPColName(cursor.getString(offset + 3));
        entity.setPColId(cursor.getString(offset + 4));
        entity.setChalNum(cursor.getInt(offset + 5));
        entity.setPageNum(cursor.getInt(offset + 6));
        entity.setPType(cursor.getInt(offset + 7));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(LiveCKindObj entity, long rowId) {
        entity.setKeyL(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(LiveCKindObj entity) {
        if(entity != null) {
            return entity.getKeyL();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(LiveCKindObj entity) {
        return entity.getKeyL() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
