package com.db.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.datas.CacheKindObj;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "CACHE_KIND_OBJ".
*/
public class CacheKindObjDao extends AbstractDao<CacheKindObj, Long> {

    public static final String TABLENAME = "CACHE_KIND_OBJ";

    /**
     * Properties of entity CacheKindObj.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property KeyL = new Property(0, Long.class, "keyL", true, "_id");
        public final static Property ColName = new Property(1, String.class, "colName", false, "COL_NAME");
    }


    public CacheKindObjDao(DaoConfig config) {
        super(config);
    }
    
    public CacheKindObjDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"CACHE_KIND_OBJ\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: keyL
                "\"COL_NAME\" TEXT NOT NULL );"); // 1: colName
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_CACHE_KIND_OBJ_COL_NAME ON \"CACHE_KIND_OBJ\"" +
                " (\"COL_NAME\" ASC);");
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"CACHE_KIND_OBJ\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, CacheKindObj entity) {
        stmt.clearBindings();
 
        Long keyL = entity.getKeyL();
        if (keyL != null) {
            stmt.bindLong(1, keyL);
        }
        stmt.bindString(2, entity.getColName());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, CacheKindObj entity) {
        stmt.clearBindings();
 
        Long keyL = entity.getKeyL();
        if (keyL != null) {
            stmt.bindLong(1, keyL);
        }
        stmt.bindString(2, entity.getColName());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public CacheKindObj readEntity(Cursor cursor, int offset) {
        CacheKindObj entity = new CacheKindObj( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // keyL
            cursor.getString(offset + 1) // colName
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, CacheKindObj entity, int offset) {
        entity.setKeyL(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setColName(cursor.getString(offset + 1));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(CacheKindObj entity, long rowId) {
        entity.setKeyL(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(CacheKindObj entity) {
        if(entity != null) {
            return entity.getKeyL();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(CacheKindObj entity) {
        return entity.getKeyL() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
